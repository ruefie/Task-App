import { Chart as ChartJS } from 'chart.js/auto';
// If you use plugins, register them here, e.g.:
// import annotationPlugin from 'chartjs-plugin-annotation';
// ChartJS.register(annotationPlugin);

export default ChartJS;
